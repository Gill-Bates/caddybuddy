{"config":{"lang":["en"],"separator":"[\\s\\-\\.]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"CaddyBuddy","text":"<p> Manage one Caddy installation through a focused web interface for sites, configuration, certificates, and SSL Labs assessments. </p> <p>Get started View on GitHub</p>"},{"location":"#what-caddybuddy-manages","title":"What CaddyBuddy manages","text":"<ul> <li> <p> Caddy runtime</p> <p>Guided onboarding verifies the Admin API and establishes CaddyBuddy as the configuration manager.</p> <p>Connect Caddy</p> </li> <li> <p> Sites</p> <p>Create, validate, enable, disable, and deploy site blocks with one or more domains.</p> <p>Manage sites</p> </li> <li> <p> Caddyfile</p> <p>Edit the managed configuration, validate syntax, and deploy changes through Caddy's Admin API.</p> <p>Use the editor</p> </li> <li> <p> Certificates</p> <p>See certificate state and remaining validity for configured domains, and trigger renewals when runtime control is available.</p> <p>Dashboard overview</p> </li> <li> <p> SSL Labs</p> <p>Register an email address, run assessments, schedule weekly scans, and inspect grade history.</p> <p>Configure SSL Labs</p> </li> <li> <p> Protected administration</p> <p>Session authentication, CSRF validation, security headers, rate limiting, and constrained Caddy Admin targets are built in.</p> <p>Security guidance</p> </li> </ul>"},{"location":"#deployment-model","title":"Deployment model","text":"<p>CaddyBuddy is designed for a single Caddy installation. The recommended deployment uses the published Docker image, a persistent application data directory, access to a private Caddy Admin API, and optional read access to Caddy's certificate storage.</p> <p>Exclusive configuration ownership</p> <p>After onboarding, treat CaddyBuddy as the owner of the active Caddy configuration. Avoid editing the managed Caddyfile concurrently with another deployment tool.</p>"},{"location":"#useful-links","title":"Useful links","text":"<ul> <li>Docker Hub</li> <li>GitHub releases</li> <li>Issue tracker</li> <li>Changelog</li> </ul>"},{"location":"changelog/","title":"Changelog","text":"<p>This page is generated from the repository-level <code>CHANGELOG.md</code> during the documentation build.</p> <p>For the current source version, see the repository changelog.</p>"},{"location":"license/","title":"License","text":"<p>CaddyBuddy is distributed under the GNU Affero General Public License v3.0.</p> <p>Refer to the GNU AGPL-3.0 license text for the complete terms.</p>"},{"location":"troubleshooting/","title":"Troubleshooting","text":""},{"location":"troubleshooting/#the-container-does-not-start","title":"The container does not start","text":"<p>Check the logs:</p> <pre><code>docker compose -f docker/docker-compose.yml.example logs caddybuddy\n</code></pre> <p>Verify that <code>CB_SECRET_KEY</code> is set and sufficiently long, the <code>data</code> directory is writable, and <code>/etc/caddy/Caddyfile</code> is a file rather than a directory.</p>"},{"location":"troubleshooting/#readiness-returns-503","title":"Readiness returns 503","text":"<pre><code>curl -i http://127.0.0.1:8000/api/v1/ready\n</code></pre> <p>Common causes:</p> <ul> <li>onboarding has not completed;</li> <li>the Caddy Admin API address is wrong;</li> <li>the API is not reachable from the container;</li> <li>Caddy is not running;</li> <li>the Admin API is bound only to a namespace that CaddyBuddy cannot access.</li> </ul> <p>For a host Caddy instance from Docker, try <code>http://host.docker.internal:2019</code> and keep the <code>host-gateway</code> mapping from the example Compose file.</p>"},{"location":"troubleshooting/#the-caddyfile-mount-is-a-directory","title":"The Caddyfile mount is a directory","text":"<p>Stop the container, remove the mistakenly created directory, create the file, and start again:</p> <pre><code>docker compose -f docker/docker-compose.yml.example down\nsudo rm -rf /etc/caddy/Caddyfile\nsudo install -m 0644 /dev/null /etc/caddy/Caddyfile\ndocker compose -f docker/docker-compose.yml.example up -d\n</code></pre> <p>Review the file path before running <code>rm -rf</code>.</p>"},{"location":"troubleshooting/#certificate-data-is-missing","title":"Certificate data is missing","text":"<p>Confirm that Caddy's storage directory is mounted into CaddyBuddy and that the container user can read it. Custom Caddy storage locations require the matching <code>CB_CADDY_CERTIFICATES_PATH</code>.</p>"},{"location":"troubleshooting/#ssl-labs-scans-fail","title":"SSL Labs scans fail","text":"<p>The domain must be a public hostname with a publicly reachable HTTPS service. Private addresses, local names, and URLs are rejected. Also verify the SSL Labs registration status under Settings.</p>"},{"location":"troubleshooting/#login-or-form-submissions-fail-behind-https","title":"Login or form submissions fail behind HTTPS","text":"<p>Ensure the reverse proxy sends <code>X-Forwarded-Proto</code> and that <code>FORWARDED_ALLOW_IPS</code> trusts the proxy's actual source address. Do not trust arbitrary public clients.</p>"},{"location":"configuration/caddy-runtime/","title":"Caddy Runtime","text":""},{"location":"configuration/caddy-runtime/#admin-api","title":"Admin API","text":"<p>CaddyBuddy deploys configuration through Caddy's Admin API. The API must be reachable from the application but must not be exposed publicly.</p> <p>Supported target names are intentionally constrained to local and deployment-internal hosts such as <code>localhost</code>, <code>host.docker.internal</code>, and <code>caddy</code>, plus private or internal addresses accepted by validation.</p> <p>Typical Docker-to-host configuration:</p> <pre><code>http://host.docker.internal:2019\n</code></pre> <p>The example Compose file maps <code>host.docker.internal</code> through Docker's <code>host-gateway</code>.</p>"},{"location":"configuration/caddy-runtime/#caddyfile","title":"Caddyfile","text":"<p>The default container path is:</p> <pre><code>/app/Caddyfile\n</code></pre> <p>The mounted path must point to a file named <code>Caddyfile</code>. It must be writable when CaddyBuddy imports, backs up, or replaces an existing host configuration.</p>"},{"location":"configuration/caddy-runtime/#certificate-storage","title":"Certificate storage","text":"<p>Mount Caddy's storage directory when certificate inspection and renewal monitoring are required:</p> <pre><code>volumes:\n  - /var/lib/caddy/.local/share/caddy:/var/lib/caddy/.local/share/caddy\n</code></pre>"},{"location":"configuration/caddy-runtime/#runtime-control-modes","title":"Runtime control modes","text":"<p>CaddyBuddy can operate without direct service control, or use:</p> <ul> <li><code>systemd</code> for a host unit;</li> <li><code>docker</code> for a named container;</li> <li><code>script</code> for a deployment-specific control command.</li> </ul> <p>Runtime control is separate from configuration loading through the Admin API. Keep it disabled unless restart or renewal workflows require it.</p>"},{"location":"configuration/caddy-runtime/#trusted-proxies","title":"Trusted proxies","text":"<p>The baseline Caddyfile contains a disabled trusted-proxy example. Enable it only when Caddy runs behind a trusted proxy or CDN:</p> <pre><code>servers {\n    trusted_proxies static private_ranges\n    trusted_proxies_strict\n}\n</code></pre>"},{"location":"configuration/environment/","title":"Environment Variables","text":"<p>Most runtime integration settings are managed in the web UI after onboarding. Environment variables configure process-level behavior and advanced deployment options.</p>"},{"location":"configuration/environment/#common-variables","title":"Common variables","text":"Variable Default Purpose <code>CB_SECRET_KEY</code> insecure placeholder Session signing secret; must be at least 32 characters in production <code>LOG_LEVEL</code> <code>INFO</code> Application log level <code>TZ</code> <code>UTC</code> IANA timezone such as <code>Europe/Berlin</code> <code>HOST</code> <code>0.0.0.0</code> Listen address <code>PORT</code> <code>8000</code> Listen port <code>FORWARDED_ALLOW_IPS</code> <code>127.0.0.1</code> Trusted proxy addresses used for forwarded headers <code>DATABASE_URL</code> <code>sqlite+aiosqlite:///data/caddybuddy.db</code> SQLAlchemy database URL <code>CB_CADDY_CERTIFICATES_PATH</code> Caddy default storage path Certificate directory exposed to CaddyBuddy <p>Generate a session secret with:</p> <pre><code>head -c 32 /dev/urandom | base64\n</code></pre>"},{"location":"configuration/environment/#session-settings","title":"Session settings","text":"Variable Default Purpose <code>SESSION_HTTPS_ONLY</code> <code>true</code> Send the session cookie only over HTTPS <code>SESSION_SAMESITE</code> <code>lax</code> Session cookie SameSite policy <code>PASSWORD_PEPPER</code> unset Optional additional password secret <p>Do not disable HTTPS-only cookies in production.</p>"},{"location":"configuration/environment/#caddy-runtime-control","title":"Caddy runtime control","text":"Variable Default Purpose <code>CB_CADDY_CONTROL_MODE</code> <code>disabled</code> <code>disabled</code>, <code>systemd</code>, <code>docker</code>, or <code>script</code> <code>CB_CADDY_SYSTEMD_UNIT</code> <code>caddy</code> systemd unit used for runtime control <code>CB_CADDY_DOCKER_CONTAINER</code> <code>caddy</code> Docker container used for runtime control <code>CB_CADDY_CONTROL_SCRIPT</code> <code>/app/caddy-control.sh</code> Custom control script <code>CB_CADDY_CONTROL_TIMEOUT_SECONDS</code> <code>30</code> Runtime-control timeout <code>CB_CADDY_RESTART_CONFIRMATION_REQUIRED</code> <code>true</code> Require confirmation before restarting Caddy"},{"location":"configuration/environment/#ssl-labs","title":"SSL Labs","text":"Variable Default Purpose <code>SSLLABS_API_BASE_URL</code> <code>https://api.ssllabs.com/api/v4</code> SSL Labs API endpoint <code>SSLLABS_TIMEOUT_SECONDS</code> <code>20</code> Request timeout <code>SSLLABS_CACHE_MAX_AGE_HOURS</code> <code>24</code> Cached registration-state lifetime <p>The registration email and history retention are configured in the web UI.</p>"},{"location":"development/architecture/","title":"Architecture","text":"<p>CaddyBuddy is a server-rendered FastAPI application backed by SQLAlchemy and SQLite.</p>"},{"location":"development/architecture/#main-layers","title":"Main layers","text":"Layer Location Responsibility Configuration <code>app/config</code> Environment validation, logging, limiter configuration Routers <code>app/routers</code> HTML pages and JSON endpoints Services <code>app/services</code> Caddy integration, onboarding, SSL Labs, certificates, events Repositories <code>app/repositories</code> Database access Models and schemas <code>app/models</code>, <code>app/schemas</code> Persistence and response contracts Templates and assets <code>app/templates</code>, <code>app/static</code> Server-rendered UI and browser behavior"},{"location":"development/architecture/#configuration-flow","title":"Configuration flow","text":"<ol> <li>Site and runtime settings are stored in SQLite.</li> <li>CaddyBuddy assembles the baseline, snippets, and enabled site definitions.</li> <li>The candidate Caddyfile is validated.</li> <li>The configuration is loaded through the private Caddy Admin API.</li> <li>The managed Caddyfile and runtime state are reconciled.</li> </ol>"},{"location":"development/architecture/#live-updates","title":"Live updates","text":"<p>The application uses server-sent events to notify authenticated browser sessions about resource changes. Dashboard and related views refresh their data after receiving an event.</p>"},{"location":"development/architecture/#design-boundary","title":"Design boundary","text":"<p>CaddyBuddy manages a single Caddy installation. It is not a multi-node orchestrator and does not replace host-level backup, firewall, monitoring, or secret-management systems.</p>"},{"location":"development/setup/","title":"Local Development","text":""},{"location":"development/setup/#environment","title":"Environment","text":"<p>CaddyBuddy requires Python 3.13.</p> <pre><code>git clone https://github.com/Gill-Bates/caddybuddy.git\ncd caddybuddy\n\npython3.13 -m venv .venv\n. .venv/bin/activate\npython -m pip install --upgrade pip\npython -m pip install -r requirements.txt\n</code></pre> <p>Start the application:</p> <pre><code>export CB_SECRET_KEY=\"$(head -c 32 /dev/urandom | base64)\"\nexport LOG_LEVEL=DEBUG\npython run.py\n</code></pre>"},{"location":"development/setup/#tests","title":"Tests","text":"<p>Run the Python test suite:</p> <pre><code>python -m pytest\n</code></pre> <p>UI lint tooling is maintained under <code>tools/ui-lint</code> and uses Playwright:</p> <pre><code>cd tools/ui-lint\nnpm install\nnpx playwright install chromium firefox webkit\nnpm run audit\n</code></pre> <p>The browser audit expects a running CaddyBuddy instance and credentials provided through its documented environment variables.</p>"},{"location":"development/setup/#documentation","title":"Documentation","text":"<pre><code>python -m pip install -r docs/requirements-docs.txt\ncp CHANGELOG.md docs/changelog.md\nmkdocs serve -f docs/mkdocs.yml\n</code></pre> <p>Open <code>http://127.0.0.1:8000</code> for the documentation preview unless another port is selected.</p>"},{"location":"features/caddyfile/","title":"Caddyfile Editor","text":"<p>The Caddyfile page exposes the managed global configuration assembled by CaddyBuddy.</p>"},{"location":"features/caddyfile/#validate-before-deployment","title":"Validate before deployment","text":"<p>The editor validates Caddyfile syntax before loading changes into Caddy. Invalid input is rejected without replacing the active runtime configuration.</p>"},{"location":"features/caddyfile/#managed-content","title":"Managed content","text":"<p>The generated configuration combines:</p> <ul> <li>the global options block;</li> <li>reusable snippets;</li> <li>enabled site blocks stored in CaddyBuddy.</li> </ul> <p>The global ACME email and Caddy Admin endpoint are managed settings. Site blocks should be edited through Sites when possible so the database and generated file stay consistent.</p>"},{"location":"features/caddyfile/#default-runtime-logging","title":"Default runtime logging","text":"<p>Fresh configurations include central Caddy runtime logging:</p> <pre><code>log {\n    level info\n    format json\n    output file /var/log/caddy/runtime.json {\n        roll_size 10MiB\n        roll_keep 5\n        roll_keep_for 168h\n    }\n}\n</code></pre> <p>This is global runtime logging, not per-site access logging. Add site-specific <code>log</code> directives only when individual access logs are required.</p>"},{"location":"features/caddyfile/#safe-editing","title":"Safe editing","text":"<p>Warning</p> <p>Do not edit the mounted Caddyfile concurrently outside CaddyBuddy. External writes can be overwritten by the next deployment and can leave stored site state out of sync with the file.</p>"},{"location":"features/dashboard/","title":"Dashboard","text":"<p>The dashboard provides a compact operational view of the connected Caddy installation.</p>"},{"location":"features/dashboard/#runtime-state","title":"Runtime state","text":"<p>It reports:</p> <ul> <li>Caddy service status;</li> <li>detected Caddy version;</li> <li>service uptime;</li> <li>configured and enabled domain counts;</li> <li>valid, expired, and expiring certificate counts.</li> </ul> <p>The page receives resource updates through a server-sent events stream and refreshes metrics without requiring a full page reload.</p>"},{"location":"features/dashboard/#ssl-labs-rank-history","title":"SSL Labs rank history","text":"<p>When SSL Labs is configured, the dashboard shows grade distribution and rank history for assessed domains. Available time ranges depend on the retained history.</p> <p>The retention setting is managed under Settings \u2192 SSL Labs History Retention. Older samples are removed automatically.</p>"},{"location":"features/dashboard/#certificate-data","title":"Certificate data","text":"<p>Certificate status is derived from Caddy's mounted certificate storage. If the storage path is unavailable, CaddyBuddy continues to manage sites but cannot provide complete certificate information.</p>"},{"location":"features/sites/","title":"Sites","text":"<p>The Sites page manages Caddy site blocks and their deployment state.</p>"},{"location":"features/sites/#create-a-site","title":"Create a site","text":"<p>Provide:</p> <ul> <li>a descriptive site name;</li> <li>one or more domains;</li> <li>the Caddy directives that belong inside the site block;</li> <li>whether the site is enabled.</li> </ul> <p>Use Validate before saving to check the generated configuration. Create &amp; Deploy stores the site and loads the updated Caddy configuration.</p>"},{"location":"features/sites/#multiple-domains","title":"Multiple domains","text":"<p>A site can contain multiple normalized domains. Certificate state is displayed for the configured names when Caddy's certificate storage is mounted.</p>"},{"location":"features/sites/#edit-or-disable-a-site","title":"Edit or disable a site","text":"<p>Editing and deploying replaces the generated site block. Disabling a site keeps its definition in CaddyBuddy but omits it from the active configuration.</p>"},{"location":"features/sites/#delete-a-site","title":"Delete a site","text":"<p>Deletion removes the stored site and deploys the resulting configuration. Review the confirmation dialog carefully because the active Caddy configuration changes immediately.</p>"},{"location":"features/sites/#certificate-renewal","title":"Certificate renewal","text":"<p>The renewal action is available when CaddyBuddy has certificate storage access and a configured runtime control mode. CaddyBuddy monitors the resulting certificate state instead of assuming that a restart completed the renewal.</p>"},{"location":"features/ssl-labs/","title":"SSL Labs","text":"<p>CaddyBuddy integrates with the Qualys SSL Labs API for public HTTPS endpoints.</p>"},{"location":"features/ssl-labs/#configure-registration","title":"Configure registration","text":"<p>Open Settings, enter the email address used for SSL Labs API requests, and register it. CaddyBuddy stores the address and displays a masked version after saving.</p> <p>Leaving the email empty disables SSL Labs integration.</p>"},{"location":"features/ssl-labs/#run-assessments","title":"Run assessments","text":"<p>The SSL Labs page lists public domains from enabled sites. You can:</p> <ul> <li>start a fresh assessment;</li> <li>inspect the latest grade and endpoint details;</li> <li>open the full report on SSL Labs;</li> <li>enable or disable the weekly schedule for each domain.</li> </ul> <p>Only concrete public hostnames are accepted. Local names, private addresses, URLs, and malformed hostnames are rejected.</p>"},{"location":"features/ssl-labs/#rank-history","title":"Rank history","text":"<p>Assessment grades are sampled for the dashboard history. Rank-history retention can be set between 30 days and one year in Settings.</p>"},{"location":"features/ssl-labs/#external-service-considerations","title":"External-service considerations","text":"<p>SSL Labs assessments are performed by an external service and can take several minutes. The target must be publicly reachable on HTTPS, and API availability or rate limits can delay results.</p>"},{"location":"getting-started/first-run/","title":"First Run","text":""},{"location":"getting-started/first-run/#administrator-setup","title":"Administrator setup","text":"<p>On the first start with an empty database, CaddyBuddy asks you to create the administrator password in the browser. The username is <code>admin</code>.</p> <p>The password must include:</p> <ul> <li>at least eight characters;</li> <li>an uppercase letter;</li> <li>a lowercase letter;</li> <li>a digit;</li> <li>a special character.</li> </ul>"},{"location":"getting-started/first-run/#onboarding-modes","title":"Onboarding modes","text":"<p>The wizard supports Caddy running on the same host, in Docker, or being prepared for later installation. Available actions depend on whether CaddyBuddy can reach the Admin API and read or write the configured Caddyfile.</p>"},{"location":"getting-started/first-run/#existing-configuration","title":"Existing configuration","text":"<p>When importing an existing Caddyfile, CaddyBuddy checks the file, creates a backup during takeover, and switches management to its generated configuration.</p>"},{"location":"getting-started/first-run/#fresh-baseline","title":"Fresh baseline","text":"<p>The default baseline includes:</p> <ul> <li>global JSON runtime logging with rotation;</li> <li>reusable standard and strict security-header snippets;</li> <li>the global ACME email once configured.</li> </ul>"},{"location":"getting-started/first-run/#admin-api-repair","title":"Admin API repair","text":"<p>For supported host deployments, the wizard can add the Admin API directive to the Caddyfile and restart Caddy after explicit confirmation.</p> <p>Configuration ownership</p> <p>The final wizard step confirms that CaddyBuddy becomes the exclusive manager of the active Caddy configuration. Keep other automation from writing to the same Caddyfile.</p>"},{"location":"getting-started/first-run/#after-onboarding","title":"After onboarding","text":"<p>Use Settings to configure the Caddy Admin URL, mounted Caddyfile path, rate limiting, SSL Labs registration, and rank-history retention.</p> <p>Then create the first site under Sites or review the generated configuration under Caddyfile.</p>"},{"location":"getting-started/installation/","title":"Installation","text":""},{"location":"getting-started/installation/#docker-compose","title":"Docker Compose","text":"<p>Docker is the recommended production path. The example file at <code>docker/docker-compose.yml.example</code> publishes CaddyBuddy only on the host loopback interface:</p> <pre><code>ports:\n  - \"127.0.0.1:8000:8000\"\n</code></pre> <p>It mounts:</p> Host path Container path Purpose <code>/etc/caddy/Caddyfile</code> <code>/app/Caddyfile</code> Managed Caddyfile <code>./data</code> <code>/app/data</code> SQLite database and runtime state <code>/var/lib/caddy/.local/share/caddy</code> same path Certificate inspection and renewal monitoring <p>Pull and start the image:</p> <pre><code>export CB_SECRET_KEY=\"$(head -c 32 /dev/urandom | base64)\"\ndocker compose -f docker/docker-compose.yml.example pull\ndocker compose -f docker/docker-compose.yml.example up -d\n</code></pre> <p>Create bind-mounted files first</p> <p>Docker creates a directory when a bind-mounted source file does not exist. Ensure <code>/etc/caddy/Caddyfile</code> is an existing file before starting the container.</p>"},{"location":"getting-started/installation/#reverse-proxy","title":"Reverse proxy","text":"<p>Expose CaddyBuddy through HTTPS and preserve the original scheme:</p> <pre><code>caddybuddy.example.com {\n    reverse_proxy 127.0.0.1:8000 {\n        header_up Host {host}\n        header_up X-Forwarded-Proto {scheme}\n    }\n}\n</code></pre> <p>Set <code>FORWARDED_ALLOW_IPS</code> to the address or network of the trusted reverse proxy. The Compose example uses <code>\"*\"</code> as an explicit deployment placeholder; narrow it in production.</p>"},{"location":"getting-started/installation/#local-development","title":"Local development","text":"<pre><code>python3.13 -m venv .venv\n. .venv/bin/activate\npython -m pip install --upgrade pip\npython -m pip install -r requirements.txt\n\nexport CB_SECRET_KEY=\"$(head -c 32 /dev/urandom | base64)\"\nexport LOG_LEVEL=DEBUG\npython run.py\n</code></pre> <p>The development server listens on <code>http://127.0.0.1:8000</code> by default.</p>"},{"location":"getting-started/installation/#upgrade","title":"Upgrade","text":"<p>For image-based deployments:</p> <pre><code>docker compose -f docker/docker-compose.yml.example pull\ndocker compose -f docker/docker-compose.yml.example up -d --force-recreate\n</code></pre> <p>Persist and back up the <code>data</code> directory before upgrading.</p>"},{"location":"getting-started/quick-start/","title":"Quick Start","text":"<p>The repository includes a Compose example for the published multi-architecture image.</p>"},{"location":"getting-started/quick-start/#prerequisites","title":"Prerequisites","text":"<ul> <li>Linux on <code>amd64</code> or <code>arm64</code></li> <li>Docker Engine with Docker Compose</li> <li>A running Caddy 2 installation, or a Caddy installation that will be connected later</li> <li>A private network path from CaddyBuddy to the Caddy Admin API</li> </ul>"},{"location":"getting-started/quick-start/#start-caddybuddy","title":"Start CaddyBuddy","text":"<ol> <li> <p>Clone the repository:</p> <pre><code>git clone https://github.com/Gill-Bates/caddybuddy.git\ncd caddybuddy\n</code></pre> </li> <li> <p>Generate a strong session secret:</p> <pre><code>export CB_SECRET_KEY=\"$(head -c 32 /dev/urandom | base64)\"\n</code></pre> </li> <li> <p>Ensure the paths used by the Compose example exist:</p> <pre><code>mkdir -p data\nsudo test -f /etc/caddy/Caddyfile\n</code></pre> </li> <li> <p>Start the application:</p> <pre><code>docker compose -f docker/docker-compose.yml.example up -d\n</code></pre> </li> <li> <p>Open <code>http://127.0.0.1:8000</code>.</p> </li> </ol>"},{"location":"getting-started/quick-start/#complete-the-first-run","title":"Complete the first run","text":"<p>An empty database opens the account setup screen. Create the <code>admin</code> account with a password containing at least eight characters, uppercase and lowercase letters, a digit, and a special character.</p> <p>The onboarding wizard then:</p> <ol> <li>identifies where Caddy runs;</li> <li>chooses whether to import an existing configuration or start from the baseline;</li> <li>verifies Admin API and file access;</li> <li>shows the takeover summary before execution.</li> </ol> <p>Continue with First Run for the onboarding details.</p>"},{"location":"getting-started/quick-start/#verify-the-service","title":"Verify the service","text":"<pre><code>curl http://127.0.0.1:8000/api/v1/health\ncurl http://127.0.0.1:8000/api/v1/ready\n</code></pre> <p><code>/health</code> confirms that the web process is running. <code>/ready</code> also requires completed onboarding and a reachable Caddy Admin API.</p>"},{"location":"operations/health-checks/","title":"Health Checks","text":"<p>CaddyBuddy exposes unauthenticated liveness and readiness endpoints for container and service monitoring.</p>"},{"location":"operations/health-checks/#liveness","title":"Liveness","text":"<pre><code>curl --fail http://127.0.0.1:8000/api/v1/health\n</code></pre> <p>The endpoint returns HTTP <code>200</code> when the web process can serve requests.</p>"},{"location":"operations/health-checks/#readiness","title":"Readiness","text":"<pre><code>curl --fail http://127.0.0.1:8000/api/v1/ready\n</code></pre> <p>Readiness returns HTTP <code>200</code> only when:</p> <ul> <li>runtime status can be evaluated;</li> <li>onboarding is complete;</li> <li>the configured Caddy Admin API is reachable.</li> </ul> <p>It returns HTTP <code>503</code> while one of these conditions is not satisfied.</p>"},{"location":"operations/health-checks/#build-information","title":"Build information","text":"<pre><code>curl http://127.0.0.1:8000/api/v1/build-info\n</code></pre> <p>This endpoint reports the application version and build metadata and can be used to confirm a deployment upgrade.</p>"},{"location":"operations/security/","title":"Security","text":""},{"location":"operations/security/#deployment-rules","title":"Deployment rules","text":"<ul> <li>Put CaddyBuddy behind HTTPS.</li> <li>Keep the Caddy Admin API on loopback, a private network, or an internal Docker network.</li> <li>Bind the application port to loopback unless direct network access is intentional.</li> <li>Persist and protect the <code>data</code> directory because it contains the database and application state.</li> <li>Use a unique random <code>CB_SECRET_KEY</code> of at least 32 characters.</li> <li>Restrict <code>FORWARDED_ALLOW_IPS</code> to trusted reverse proxies.</li> <li>Keep <code>SESSION_HTTPS_ONLY=true</code> in production.</li> </ul>"},{"location":"operations/security/#application-protections","title":"Application protections","text":"<p>CaddyBuddy includes:</p> <ul> <li>authenticated sessions with inactivity and absolute timeouts;</li> <li>CSRF tokens and origin validation for state-changing browser requests;</li> <li>security response headers;</li> <li>rate limiting for sensitive endpoints;</li> <li>password complexity validation;</li> <li>constrained Caddy Admin API targets;</li> <li>validation before Caddy configuration deployment.</li> </ul>"},{"location":"operations/security/#reverse-proxy-headers","title":"Reverse-proxy headers","text":"<p>Forward the original host and scheme:</p> <pre><code>reverse_proxy 127.0.0.1:8000 {\n    header_up Host {host}\n    header_up X-Forwarded-Proto {scheme}\n}\n</code></pre> <p>Incorrect forwarded-header trust can allow clients to spoof request metadata. Trust only the proxy addresses that actually connect to CaddyBuddy.</p>"},{"location":"operations/security/#configuration-ownership","title":"Configuration ownership","text":"<p>CaddyBuddy is designed to own the active configuration after onboarding. Concurrent writers can bypass validation, overwrite generated content, or create a mismatch between the database and Caddyfile.</p>"},{"location":"operations/security/#reporting-vulnerabilities","title":"Reporting vulnerabilities","text":"<p>Do not publish secrets or exploitable details in a public issue. Contact the repository owner through the channels listed on the GitHub profile.</p>"}]}