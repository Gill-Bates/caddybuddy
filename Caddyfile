{
    # ACME/TLS email — filled in by CaddyBuddy during onboarding.
    email {{ ACME_EMAIL }}

    log {
        level info
        format json
        output stdout
    }

    # Nur aktivieren, wenn Caddy hinter einem vertrauenswürdigen Proxy/CDN läuft.
    #
    servers {
    #     trusted_proxies static private_ranges
    #     trusted_proxies_strict
    }
}

(security_headers) {
    header {
        Strict-Transport-Security "max-age=31536000"
        X-Content-Type-Options "nosniff"
        # X-Frame-Options "DENY"
        Referrer-Policy "strict-origin-when-cross-origin"

        -Server
        -X-Powered-By
    }
}

(security_headers_strict) {
    header {
        Strict-Transport-Security "max-age=31536000"
        X-Content-Type-Options "nosniff"
        X-Frame-Options "DENY"
        Referrer-Policy "strict-origin-when-cross-origin"

        -Server
        -X-Powered-By
    }
}

(default_log) {
    log {
        output stdout
        format json
    }
}